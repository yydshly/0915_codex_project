# 项目图片

存放真实项目截图、架构图与演示 GIF，文件名使用小写英文和短横线。

- 推荐封面：`cover.png` 或 `cover.webp`，使用约 16:9 的画面。
- 其他图片：按内容命名，如 `workflow.png`、`settings-panel.png`。
- 在研究 README 中给图片添加替代文字、简短说明及必要的来源。
- `project.json` 的 `cover` 使用相对子项目根目录的路径，例如 `assets/cover.png`。
- 暂无图片时保持 `cover` 为空，首页会显示“截图待补充”，不引用不存在的图片。
